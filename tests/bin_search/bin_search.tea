use std;

fn main() -> i32
{   
    std::timer_start(3);
    let i:i32;
    let sum:i32;
    let a: [i32; 10];
    sum=0;
    i=0;
    while i<10
    {
        a[i]=i+1;
        i=i+1;
    }
    let x:i32;
    let high:i32;
    let low:i32;
    let mid:i32;
    let n:i32;
    n=10;
    x=std::getint();
    high=n-1;
    low=0;
    mid=(high+low)/2;
    while (a[mid]!=x) && (low < high)
    {
        mid=(high+low)/2;
        if x<a[mid]
        {
           high=mid-1; 
        }
        else
        {
            low = mid +1;
        }    
    }

    if x==a[mid]
    {
        std::putint(x);
    }
    else
    {
        x = 0;
        std::putint(x);
    }
    
    x = 10;
    std::putch(x);
    std::timer_stop(49);
    return 0;
}