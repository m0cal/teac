use std;

fn canJump(nums: &[i32], n:i32) -> i32 {
    if n == 1
    {
        return 1;
    }
    if nums[0] > (n - 2)
    {
        return 1;
    }
    let dp: [i32; 10];
    let i:i32;
    i = 0;
    while i<(n-1){
        dp[i] = 0;
        i = i+1;
    }
    let temp:i32 = n - 1;
    dp[temp] = 1;
    i = n - 2;
    while i > -1{
        let j:i32;
        if nums[i] < (n - 1 - i){
            j = nums[i];
        }
        else
        {
            j = n - 1 - i;
        }
        while j > -1{
            temp = i+j;
            if dp[temp] != 0{
                dp[i] = 1;
            }
            j = j - 1;
        }
        i = i - 1;
    }

    return dp[0];
}

fn main() -> i32{
    std::timer_start(42);
    let res:i32;
    let a: [i32; 10];
    a[0]=3;a[1]=3;a[2]=9;a[3]=0;a[4]=0;
    a[5]=1;a[6]=1;a[7]=5;a[8]=7;a[9]=8;
    res = 10;
    res = canJump(a, res);
    std::timer_stop(49);
    return res;
}