use std;

fn uniquePaths(m:i32, n:i32) -> i32 {
    if (m == 1) || (n == 1)
    {
        return 1;
    }

    let dp: [i32; 9];
    let i:i32;
    let j:i32;
    i = 0;
    let temp1:i32;
    let temp2:i32;
    let temp3:i32;
    while i<m{
        temp1 = i*3+n-1;
        dp[temp1]=1;
        i = i + 1;
    }
    i = 0;
    while i<n{
        temp1 = (m-1)*3+i;
        dp[temp1]=1;
        i = i + 1;
    }
    i=m-2;
    while i > -1{
        j=n-2;
        while j > -1{
            temp1 = i*3+j;
            temp2 = (i+1)*3+j;
            temp3 = i*3+j+1;
            dp[temp1] = dp[temp2] + dp[temp3];
            j = j - 1;
        }
        i = i - 1;
    }
    return dp[0];
}
fn main() -> i32{
    std::timer_start(40);
    let res:i32;
    let n:i32;
    n=3;
    res = uniquePaths(n, n);
    std::timer_stop(45);
    return res;
}